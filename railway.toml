[build]
builder = "dockerfile"
dockerfile = "apps/api/Dockerfile"

[env]
ENVIRONMENT = "production"
LOG_LEVEL = "INFO"
PYTHONUNBUFFERED = "1"

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
